# Plain HTTP keeps this integration example self-contained.
server example {
	tls no
	bind 127.0.0.1 8000
}

load ./kore-example.so
task_threads 4
http_keepalive_time 0

domain * {
	attach example

	route /limited {
		handler limited
		methods get
	}
}
